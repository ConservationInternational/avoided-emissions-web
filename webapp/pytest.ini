[pytest]
testpaths = tests
pythonpath =
    .
    ..
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require PostgreSQL + PostGIS)
addopts = -ra --tb=short
